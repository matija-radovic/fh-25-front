import Child from './Child/Child';
import './Example.scss'; // scss for the component if exists

const Example = () => {
    return (
        <>
            <div>
                This is a example component to show how it should look like in folder and etc.
            </div>
            <Child />
        </>
    )
}

export default Example